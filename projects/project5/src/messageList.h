#ifndef MESSAGELIST_H
#define MESSAGELIST_H

#include "message.h"

// struct Node
// {
//     Message *m;
//     Node *next;
// };

// class MessageList
// {
//     private:
//         Node *head;
//     public:
//         MessageList();
//         ~MessageList();
//         bool addNewMessage(Message &message);              // returns true if message added
//         bool listMessages(char *r);                 // returns true if recipient found
//         void listAllMessages();
//         bool deleteMessage(int id);                 // returns true if id found and message deleted
// }; 


#endif